apiVersion: v1
kind: Service

metadata:
  name: employee-service
  namespace: employee-dev

spec:
  selector:
    app: employee-app

  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30081

  type: NodePort
